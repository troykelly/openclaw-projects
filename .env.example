# openclaw-projects production environment
#
# Copy to .env and edit as needed:
#   cp .env.example .env

# Public base URL used for magic-link redirects
PUBLIC_BASE_URL=https://projects.example.com

# Cookie signing secret (recommended for production; generate a long random string)
# Example:
#   openssl rand -base64 48
COOKIE_SECRET=

# Host port to publish the projects service on (bound to 127.0.0.1 in prod compose)
PROJECTS_PORT=3000

# Postgres credentials/db name
POSTGRES_USER=openclaw
POSTGRES_PASSWORD=openclaw
POSTGRES_DB=openclaw

# S3-compatible storage (SeaweedFS)
# Required for file attachments functionality
# SeaweedFS runs embedded in the compose stack on port 8333
S3_ENDPOINT=http://seaweedfs:8333
S3_BUCKET=openclaw
S3_REGION=us-east-1
S3_ACCESS_KEY=openclaw
S3_SECRET_KEY=openclaw
S3_FORCE_PATH_STYLE=true

# SeaweedFS configuration
# Maximum volume size in MB (default 30GB for production)
SEAWEEDFS_VOLUME_SIZE_LIMIT_MB=30000

# Optional: Postmark email (magic links)
# If unset, the app will still work but will return the login URL in non-production only.
# Can use either POSTMARK_TRANSACTIONAL_TOKEN directly or POSTMARK_TRANSACTIONAL_TOKEN_FILE
# POSTMARK_TRANSACTIONAL_TOKEN=your-token-here
# POSTMARK_TRANSACTIONAL_TOKEN_FILE=/path/to/token/file
# POSTMARK_FROM=Projects <projects@execdesk.ai>
# POSTMARK_REPLY_TO=quasar@execdesk.ai
# POSTMARK_MESSAGE_STREAM=outbound
