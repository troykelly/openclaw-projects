-- ============================================================
-- Migration 104 DOWN: No-op
-- The UP migration deleted corrupted rows. Those rows contained
-- only "[object Object]" and have no useful data to restore.
-- ============================================================

-- Intentionally empty — deleted data cannot be reconstructed.
